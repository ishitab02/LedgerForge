// TODO: utility helpers
// - buildEIP3009Authorization(params) → typed EIP-712 signature request
// - formatTokenAmount(amount, decimals) → string
// - checksumAddress(address) → Address
// - validateSkillEndpoint(url) → boolean
