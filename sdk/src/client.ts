// TODO: LedgerForgeClient
// - listSkills(filters?)         → SkillListing[]   (reads Bazaar API)
// - initiate(skillId, wallet)    → PaymentChallenge
// - pay(challenge, wallet)       → JobHandle
// - pollStatus(handle)           → 'pending' | 'settled' | 'failed'
// - verifyReceipt(handle)        → Receipt
